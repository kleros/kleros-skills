---
phase: 05-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .well-known/agent-skills/index.json
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]

must_haves:
  truths:
    - "D-01: Full audit — npm test passes with zero failures, update-digests reports no stale hashes, all 9 multi-surface files contain kleros-curate references as expected"
    - "D-02: Structure + digests verification only — no live install test (file structure and SHA-256 digests are the verification)"
    - "D-03: Fix-then-tag — README.md project structure tree includes kleros-curate/ with a description, all surface fixes committed before tagging"
    - "D-06: No explicit Netlify deploy verification step — deploy is automatic on push"
  artifacts:
    - path: "README.md"
      provides: "Project structure tree with kleros-curate/"
      contains: "kleros-curate/"
    - path: ".well-known/agent-skills/index.json"
      provides: "SHA-256 digest for kleros-curate/SKILL.md"
      contains: "kleros-curate"
    - path: ".claude-plugin/plugin.json"
      provides: "skills[] array with ./kleros-curate"
      contains: "./kleros-curate"
    - path: ".claude-plugin/marketplace.json"
      provides: "Catalog entry mentioning Curate"
      contains: "Curate"
  key_links:
    - from: ".well-known/agent-skills/index.json"
      to: "kleros-curate/SKILL.md"
      via: "SHA-256 digest match"
      pattern: "kleros-curate.*sha256"
    - from: ".claude-plugin/plugin.json"
      to: "kleros-curate/"
      via: "skills[] array entry"
      pattern: "./kleros-curate"
---

<objective>
Audit all 9 multi-surface publishing files for kleros-curate completeness, fix the one known gap (README.md project structure tree), run the full verification suite (npm test + update-digests), and commit a clean pre-tag state.

Purpose: Establish a verified, gap-free commit as the tagging target per D-01 and D-03.
Output: All 9 surfaces confirmed consistent, digests verified, README.md fixed, clean commit.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-05-publishing/05-CONTEXT.md
@/Users/jaybuidl/project/kleros/kleros-skills/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 9 multi-surface files and fix README.md project structure tree</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 35-45 — the project structure block being fixed)
    - .claude-plugin/plugin.json (confirm ./kleros-curate in skills[])
    - .claude-plugin/marketplace.json (confirm Curate in description)
    - SKILL.md (confirm kleros-curate in routing table and skills section)
    - openclaw-skill/SKILL.md (confirm kleros-curate in Available Skills table)
    - sitemap.xml (confirm kleros-curate URL entry)
    - .well-known/agent-skills/index.json (confirm kleros-curate entry)
    - CHANGELOG.md (confirm 2.0.0 entry documents kleros-curate)
    - index.html (confirm kleros-curate card is present — grep for data-skill-name="Kleros Curate")
  </read_first>
  <action>
    Audit each of the 9 files listed in CONTEXT.md canonical_refs section. For each, confirm the kleros-curate reference exists and matches the expected pattern below. Document any additional gaps found.

    Expected patterns per surface:
    - plugin.json: `"./kleros-curate"` in skills[] array
    - marketplace.json: "Curate" in description field
    - SKILL.md (root): "kleros-curate/SKILL.md" in routing table and skills section
    - index.html: `data-skill-name="Kleros Curate"` div with data-skill-path="kleros-curate/SKILL.md"
    - openclaw-skill/SKILL.md: "kleros-curate/SKILL.md" in Available Skills table
    - README.md: "kleros-curate/" in both Skills included list AND project structure tree
    - sitemap.xml: `<loc>https://skills.kleros.io/kleros-curate/SKILL.md</loc>`
    - agent-skills/index.json: object with "name": "kleros-curate" and a sha256 digest
    - CHANGELOG.md: kleros-curate mentioned under [2.0.0] Added section

    Fix the confirmed gap in README.md: insert `kleros-curate/         # Published skill — Curate token-curated registries (Light Curate, Stake Curate, Scout)` on a new line between the `kleros-ipfs-upload/` line and the `openclaw-skill/` line in the project structure block (lines ~40-41). Preserve the column alignment of the `#` comment character.

    If any surface beyond README.md has a missing or incorrect reference (not matching the expected pattern above), fix it in the same commit. Do not leave gaps with a note to fix later.
  </action>
  <verify>
    <automated>grep -c "kleros-curate" /Users/jaybuidl/project/kleros/kleros-skills/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md project structure block contains a line with "kleros-curate/" (grep output >= 1 for the project structure section specifically)
    - All 9 surfaces contain their expected kleros-curate reference pattern — no surface returns 0 matches on its expected grep
    - No surface has a "MISSING" or "Coming Soon" label for kleros-curate (index.html card must NOT have class="draft" or cursor:default)
    - README.md remains syntactically valid markdown — project structure block is a fenced code block, not prose
  </acceptance_criteria>
  <done>All 9 multi-surface files confirmed consistent with kleros-curate references. README.md project structure tree includes kleros-curate/. Any additional gaps found during audit are fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification suite and commit clean pre-tag state</name>
  <files>.well-known/agent-skills/index.json</files>
  <read_first>
    - package.json (confirm update-digests script path and test runner command)
    - .well-known/agent-skills/index.json (current state before digest refresh)
    - kleros-curate/SKILL.md (the file whose digest will be verified)
  </read_first>
  <action>
    Run the full verification suite in order:

    1. Run `npm test` from the repo root. All tests must pass (exit code 0). If any test fails, diagnose and fix before continuing — do not skip or ignore failures.

    2. Run `npm run update-digests` from the repo root. This script recalculates SHA-256 hashes for all SKILL.md files and updates .well-known/agent-skills/index.json in-place. After running, check if the file was modified (git diff). If modified, the pre-existing digest for kleros-curate/SKILL.md was stale — the script has now corrected it.

    3. Run `npm test` a second time to confirm tests still pass after any digest update.

    4. Stage and commit all modified files. Commit message: "chore(05): audit + fix publishing surfaces — add kleros-curate/ to README structure, refresh digests". Use `git -c commit.gpgsign=false commit`. Append `Co-Authored-By: Claude <noreply@anthropic.com>` to the commit message.

    Do not run `git push` — leave pushing and tagging to the user (per D-04).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits with code 0 and reports 0 failures (second run, post-digest-update)
    - `npm run update-digests` exits with code 0
    - `git diff HEAD -- .well-known/agent-skills/index.json` shows no uncommitted changes (digest was committed if updated)
    - `git log --oneline -1` shows the audit commit with message starting "chore(05):"
    - `git status` shows clean working tree (no untracked or modified files relevant to publishing surfaces)
  </acceptance_criteria>
  <done>npm test passes, digests verified/refreshed, all surface fixes committed. Working tree is clean and ready for manual tagging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm scripts → filesystem | update-digests writes to .well-known/agent-skills/index.json — only updates SHA-256 hashes, no network calls |
| git commit → repo | commit includes only pre-reviewed surface files, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | .well-known/agent-skills/index.json | accept | File is intentionally public; contains only skill URLs and SHA-256 content digests — no secrets |
| T-05-02 | Tampering | SHA-256 digest in index.json | mitigate | npm run update-digests recomputes from actual file content; executor must run it and commit any changes |
| T-05-03 | Information Disclosure | Committed files | mitigate | Stage only named surface files — never git add -A; review git status before commit |
| T-05-SC | Tampering | npm install | accept | No new packages installed in this plan; only existing npm scripts are run |
</threat_model>

<verification>
After plan execution:
- `npm test` passes (0 failures)
- `grep -c "kleros-curate" README.md` returns >= 2 (Skills included + project structure)
- `grep "kleros-curate" .well-known/agent-skills/index.json` returns the entry with sha256 digest
- `git status` shows clean working tree
- `git log --oneline -3` shows the audit commit
</verification>

<success_criteria>
All 9 multi-surface files contain correct kleros-curate references, npm test passes, SHA-256 digests are current, and a clean commit exists as the tagging target.
</success_criteria>

<output>
Create `.planning/phases/KS-05-publishing/05-01-SUMMARY.md` when done.
</output>
