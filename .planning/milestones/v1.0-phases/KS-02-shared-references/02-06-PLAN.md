---
phase: KS-02-shared-references
plan: 06
type: execute
wave: 5
depends_on:
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - kleros-curate/SKILL.md
autonomous: true
requirements:
  - FACT-01
  - FACT-02
  - FACT-03
  - FACT-04
  - FACT-05
  - WRIT-01
  - WRIT-02

must_haves:
  truths:
    - "SKILL.md contains a 'Common workflows' section after the Action index (per D-09)"
    - "Submit item workflow lists 5 steps in order: shared-metaevidence.md → shared-item-json.md → shared-ipfs-upload.md → shared-deposits.md → flavor reference"
    - "Challenge/remove workflow lists 4 steps in order: shared-metaevidence.md → shared-ipfs-upload.md → shared-deposits.md → flavor reference"
    - "Deploy registry workflow lists 3 steps: shared-metaevidence.md → shared-ipfs-upload.md → flavor reference"
    - "Each step includes what the agent does at that step (not just the filename)"
    - "SKILL.md line count stays under 500 / 5k words (ARCH-01)"
    - "No content from shared files is duplicated into SKILL.md (ARCH-04)"
  artifacts:
    - path: "kleros-curate/SKILL.md"
      provides: "Entry point with Common workflows section"
      contains: "Common workflows"
  key_links:
    - from: "kleros-curate/SKILL.md"
      to: "kleros-curate/references/shared-metaevidence.md"
      via: "Common workflows step 1 reference"
    - from: "kleros-curate/SKILL.md"
      to: "kleros-curate/references/shared-deposits.md"
      via: "Common workflows deposit step reference"
---

<objective>
Add the "Common workflows" section to SKILL.md's action index (per D-09). This is a minor edit —
18–22 lines appended after the existing Action index section, showing multi-step loading sequences
for Submit item, Challenge/remove, and Deploy registry.

All 5 shared files must be complete (Plans 01–05 done) before this plan executes, because the
workflows reference all shared file names.

Purpose: Agents performing multi-step operations get the loading sequence upfront without having to
infer the order from individual action index entries.

Output: kleros-curate/SKILL.md with "Common workflows" subsection added — net +18–22 lines.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-02-shared-references/02-CONTEXT.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-02-shared-references/RESEARCH.md
@/Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/SKILL.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-02-shared-references/02-05-SUMMARY.md

<interfaces>
<!-- SKILL.md current action index ends around line 104 with the ABI grep pattern -->
<!-- Insert after the Action index section, before ## Reference files -->
<!-- Do not modify any other section of SKILL.md -->

Proposed "Common workflows" content (per RESEARCH.md §SKILL.md Update and D-09):

## Common workflows

**Submit an item (any flavor):**
1. `references/shared-metaevidence.md` — fetch schema (columns) and policy URI
2. `references/shared-item-json.md` — build the item.json payload
3. `references/shared-ipfs-upload.md` — upload item.json to IPFS, get CID
4. `references/shared-deposits.md` — compute exact msg.value
5. Flavor reference (`light-curate.md` or `stake-curate.md`) — send the addItem transaction

**Challenge or remove an item:**
1. `references/shared-metaevidence.md` — fetch the applicable policy (clearing policy for removal; registration policy for challenge)
2. `references/shared-ipfs-upload.md` — upload evidence JSON to IPFS
3. `references/shared-deposits.md` — compute the challenge deposit
4. Flavor reference — send the challenge or removeItem transaction

**Deploy a new registry:**
1. `references/shared-metaevidence.md` — prepare MetaEvidence JSON (policy URI + column schema)
2. `references/shared-ipfs-upload.md` — upload MetaEvidence JSON to IPFS
3. Flavor reference — call the factory deploy function

Placement: insert between the `## Action index` section and `## Reference files` section.
The section header is `## Common workflows` (same heading level as Action index and Reference files).

ARCH-01 guard: Before writing, check current line count of SKILL.md.
  Adding 18–22 lines to a 163-line file = ~185 lines — well within the 500-line / 5k-word limit.
  If the current SKILL.md has grown beyond 450 lines, note this in the summary.

ARCH-04 guard: Do not add any ABI signatures, deposit formulas, or procedure steps that duplicate
  content from shared files. The workflows section only lists file names and one-line descriptions
  of what the agent does at each step.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Common workflows section to SKILL.md</name>
  <files>kleros-curate/SKILL.md</files>
  <action>
Insert the "Common workflows" section into SKILL.md between the `## Action index` section and the
`## Reference files` section.

Steps:
1. Read kleros-curate/SKILL.md to confirm the current line count and locate the exact insertion point
   (the blank line between `## Action index` closing content and `## Reference files`).
2. Insert the `## Common workflows` section at that point with the three workflow sequences defined
   in the context interfaces above.
3. Verify line count stays under 500 lines after insertion.

WRITING RULES:
- Imperative form for step descriptions (D-10/WRIT-01): "fetch schema", "build the item.json payload"
- Keep each step to one line: filename reference + brief action phrase
- Do not add WHY rationale here — that lives in the referenced files (WRIT-04 lean content)
- Use consistent format: backtick-quoted relative path + em-dash + action phrase

SCOPE GUARD:
- Do not modify the Non-negotiables section
- Do not modify the routing decision tree
- Do not modify the Action index entries
- Do not modify the Reference files descriptions
- Only insert the new ## Common workflows section
  </action>
  <verify>
    <automated>grep -n "Common workflows" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/SKILL.md</automated>
    <automated>wc -l /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/SKILL.md</automated>
    <automated>grep -n "shared-metaevidence\|shared-item-json\|shared-ipfs-upload\|shared-deposits" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/SKILL.md | grep -v "^.*##\|Action index\|Reference files"</automated>
  </verify>
  <done>
- "Common workflows" heading appears in SKILL.md
- Three workflows present: Submit item (5 steps), Challenge/remove (4 steps), Deploy registry (3 steps)
- Each workflow step references the correct shared file
- SKILL.md line count is under 500 lines
- No new content duplicates what lives in shared files (no formulas, no signatures)
- All other SKILL.md sections unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase; no runtime code, no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KS02-SC | Tampering | No package installs in this plan | accept | Pure file editing; no npm/pip/cargo installs |
</threat_model>

<verification>
After the task completes:

1. Section present: `grep -n "Common workflows" kleros-curate/SKILL.md`
2. All three workflows: `grep -n "Submit an item\|Challenge or remove\|Deploy a new" kleros-curate/SKILL.md`
3. Step count per workflow: visually verify Submit = 5 steps, Challenge = 4 steps, Deploy = 3 steps
4. Line count: `wc -l kleros-curate/SKILL.md` — expect < 200
5. Section placement: `grep -n "^## " kleros-curate/SKILL.md` — verify order: Action index, Common workflows, Reference files
6. No shared file content duplicated: `grep -n "submissionBaseDeposit\|eth_getLogs\|columns" kleros-curate/SKILL.md` — these should NOT appear (content lives in shared files only)
7. Non-negotiables unchanged: `grep -n "Non-negotiables" kleros-curate/SKILL.md`
</verification>

<success_criteria>
- SKILL.md has a "Common workflows" section between Action index and Reference files
- Three workflow sequences present with correct file references and step descriptions
- ARCH-01: SKILL.md stays under 500 lines / 5k words
- ARCH-04: no content from shared files duplicated into SKILL.md
- D-09 decision implemented
- All other SKILL.md sections unchanged
</success_criteria>

<output>
Create `.planning/phases/KS-02-shared-references/02-06-SUMMARY.md` when done
</output>
