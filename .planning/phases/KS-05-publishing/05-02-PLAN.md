---
phase: 05-publishing
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [PUB-04]

must_haves:
  truths:
    - "D-04: Annotated tag — git tag kleros-curate@v1.0.0 exists (after user runs provided command with GPG passphrase)"
    - "D-05: GitHub release kleros-curate@v1.0.0 exists with release body from CHANGELOG.md 2.0.0 entry — skill tag only, no retroactive v2.0.0 release"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Comparison link for kleros-curate@v1.0.0"
      contains: "kleros-curate@v1.0.0"
  key_links:
    - from: "GitHub release"
      to: "git tag kleros-curate@v1.0.0"
      via: "gh release create --tag"
      pattern: "kleros-curate@v1.0.0"
---

<objective>
Add the kleros-curate@v1.0.0 comparison link to CHANGELOG.md, provide the annotated tag commands for the user to run manually (GPG passphrase required), then create the GitHub release from the tag.

Purpose: Formalize the kleros-curate v1.0.0 release per D-03 (fix-then-tag), D-04 (annotated tag, user runs manually), D-05 (GitHub release for kleros-curate@v1.0.0 only).
Output: CHANGELOG.md updated with tag comparison link, GitHub release created at kleros-curate@v1.0.0.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-05-publishing/05-CONTEXT.md
@/Users/jaybuidl/project/kleros/kleros-skills/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CHANGELOG.md with kleros-curate@v1.0.0 comparison link and commit</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — read current comparison links section at bottom, lines 39-41)
    - .planning/phases/KS-05-publishing/05-01-SUMMARY.md (confirm plan 01 completed cleanly)
  </read_first>
  <action>
    Add a comparison link entry for kleros-curate@v1.0.0 at the bottom of CHANGELOG.md.

    Current bottom of file:
      [2.0.0]: https://github.com/kleros/kleros-skills/compare/v1.1.0...v2.0.0
      [1.1.0]: https://github.com/kleros/kleros-skills/compare/v1.0.0...v1.1.0
      [1.0.0]: https://github.com/kleros/kleros-skills/releases/tag/v1.0.0

    Add this line after the existing [2.0.0] link (insert as a new line, not replacing anything):
      [kleros-curate@v1.0.0]: https://github.com/kleros/kleros-skills/releases/tag/kleros-curate@v1.0.0

    Do NOT modify the [2.0.0], [1.1.0], or [1.0.0] comparison links — those are plugin-level version links and are independent of skill-level tags per CLAUDE.md.

    Then commit the CHANGELOG.md update using `git -c commit.gpgsign=false commit -m "chore(05): add kleros-curate@v1.0.0 changelog link" --trailer "Co-Authored-By: Claude <noreply@anthropic.com>"`.
  </action>
  <verify>
    <automated>grep "kleros-curate@v1.0.0" /Users/jaybuidl/project/kleros/kleros-skills/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a line matching: `[kleros-curate@v1.0.0]: https://github.com/kleros/kleros-skills/releases/tag/kleros-curate@v1.0.0`
    - Existing [2.0.0], [1.1.0], [1.0.0] lines are unchanged
    - `git log --oneline -1` shows a commit starting "chore(05): add kleros-curate@v1.0.0"
    - `git status` shows clean working tree
  </acceptance_criteria>
  <done>CHANGELOG.md has the kleros-curate@v1.0.0 reference link committed.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: User runs annotated tag + push (requires GPG passphrase)</name>
  <what-built>CHANGELOG.md updated and committed. Repo is in a clean, gap-free state. Ready to tag.</what-built>
  <how-to-verify>
    Run these two commands in your terminal (GPG passphrase required for the tag):

    1. Create the annotated tag:
       git tag -a kleros-curate@v1.0.0 -m 'kleros-curate v1.0.0 — first release'

    2. Push the tag to origin:
       git push origin kleros-curate@v1.0.0

    Confirm the tag was created:
       git tag --list 'kleros-curate*'
       # Expected output: kleros-curate@v1.0.0

    Confirm the tag was pushed:
       git ls-remote --tags origin 'refs/tags/kleros-curate@v1.0.0'
       # Expected output: a line with the tag ref
  </how-to-verify>
  <resume-signal>Type "tagged" once both commands succeed and the tag is visible on origin.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create GitHub release for kleros-curate@v1.0.0</name>
  <files></files>
  <read_first>
    - CHANGELOG.md (lines 8-15 — the [2.0.0] Added/Changed content to use as release body)
  </read_first>
  <action>
    Create a GitHub release using the gh CLI. The release body is derived from the CHANGELOG.md [2.0.0] entry (the "Added" bullet describing kleros-curate).

    Run the following command (construct the --notes body from the CHANGELOG [2.0.0] Added section):

      gh release create kleros-curate@v1.0.0 \
        --title "kleros-curate v1.0.0" \
        --notes "First release of the kleros-curate skill.

    ## Added
    - Published kleros-curate skill — consolidated from three draft skills (Curate Light, Stake Curate/PGTCR, Curate Scout) into a single skill covering all three Curate flavors (Light Curate / LGTCR, Stake Curate / PGTCR, Scout).
    - Routing decision tree in SKILL.md disambiguates flavor from user intent.
    - Shared reference files: ABI fragments, MetaEvidence retrieval, deposit computation, item.json construction, IPFS upload.
    - Flavor reference files: light-curate.md, stake-curate.md, scout-registries.md.

    ## Install
    /plugin marketplace add kleros/kleros-skills
    /plugin install kleros-curate@kleros-skills"

    The tag kleros-curate@v1.0.0 must already exist on origin (Task 2 must be complete) before this command will succeed.
  </action>
  <verify>
    <automated>gh release view kleros-curate@v1.0.0 --json tagName,name,url --jq '.tagName + " | " + .name + " | " + .url'</automated>
  </verify>
  <acceptance_criteria>
    - `gh release view kleros-curate@v1.0.0` exits with code 0 and shows title "kleros-curate v1.0.0"
    - Release is associated with tag kleros-curate@v1.0.0 (not a draft release)
    - Release URL is reachable: https://github.com/kleros/kleros-skills/releases/tag/kleros-curate@v1.0.0
    - Release body contains "Light Curate" and "Stake Curate" and "Scout"
  </acceptance_criteria>
  <done>GitHub release kleros-curate@v1.0.0 exists and is published with the CHANGELOG-derived release body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh CLI → GitHub API | Creates release via authenticated GitHub token — no secrets embedded in files |
| git tag → origin | Annotated tag push uses existing git remote credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | GitHub release | accept | Release created under authenticated GitHub account; tag verification via git ls-remote confirms authenticity |
| T-05-05 | Information Disclosure | Release body | accept | Release body derived from public CHANGELOG.md; contains no secrets or internal paths |
| T-05-06 | Tampering | CHANGELOG.md comparison links | mitigate | Only add new [kleros-curate@v1.0.0] line; existing plugin-level links left unchanged to prevent broken comparisons |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After plan execution:
- `git tag --list 'kleros-curate*'` returns `kleros-curate@v1.0.0`
- `gh release view kleros-curate@v1.0.0 --json tagName` returns `{"tagName":"kleros-curate@v1.0.0"}`
- `grep "kleros-curate@v1.0.0" CHANGELOG.md` returns the comparison link line
</verification>

<success_criteria>
Tag kleros-curate@v1.0.0 exists on origin, GitHub release is published with complete release notes, CHANGELOG.md has the tag reference link. kleros-curate skill is formally released.
</success_criteria>

<output>
Create `.planning/phases/KS-05-publishing/05-02-SUMMARY.md` when done.
</output>
