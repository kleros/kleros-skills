---
phase: KS-02-shared-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kleros-curate/references/shared-abi-fragments.md
autonomous: true
requirements:
  - FACT-05
  - FACT-06
  - WRIT-01
  - WRIT-02
  - WRIT-03
  - WRIT-04

must_haves:
  truths:
    - "shared-abi-fragments.md contains all LightGeneralizedTCR function signatures in Solidity-style format (per D-04)"
    - "shared-abi-fragments.md contains all PermanentGTCR function signatures in Solidity-style format (per D-04)"
    - "shared-abi-fragments.md contains the IArbitrator interface (arbitrationCost, appealCost, appealPeriod, currentRuling)"
    - "shared-abi-fragments.md contains full event signatures for MetaEvidence, NewItem, NewGTCR in Solidity-style (per D-05)"
    - "The MetaEvidence topic0 hash 0x61606860... is a comment on the MetaEvidence event — single canonical location"
    - "No JSON ABI arrays anywhere in the file (per D-06)"
    - "Factory ABIs (LightGTCRFactory, PermanentGTCRFactory) are present under ## Factory contracts, each labeled (LGTCR only)/(PGTCR only)"
    - "D-01 flavor subsection structure: ## LightGeneralizedTCR, ## PermanentGTCR, ## LightGeneralizedTCRView, ## IArbitrator, ## Factory contracts"
    - "D-02 Scout pointer appears under ## LightGeneralizedTCRView"
    - "All instructions in imperative form; constraints include WHY rationale (per D-10, D-11)"
  artifacts:
    - path: "kleros-curate/references/shared-abi-fragments.md"
      provides: "All Curate ABI signatures in Solidity-style"
      min_lines: 160
  key_links:
    - from: "kleros-curate/references/shared-metaevidence.md"
      to: "kleros-curate/references/shared-abi-fragments.md"
      via: "topic0 pointer: '(topic0 and full event signature: shared-abi-fragments.md)'"
    - from: "kleros-curate/references/shared-deposits.md"
      to: "kleros-curate/references/shared-abi-fragments.md"
      via: "arbitrator signature pointer: '(full IArbitrator signature: shared-abi-fragments.md)'"
---

<objective>
Fill the shared-abi-fragments.md stub with all Curate contract signatures in Solidity-style human-readable
format. This is the first file in the extraction dependency chain — all subsequent shared files reference
function and event names defined here.

Purpose: Provide a single authoritative ABI reference that agents can grep for any function or event
signature without loading flavor-specific files. Topic0 hash for MetaEvidence lives exclusively here.

Output: kleros-curate/references/shared-abi-fragments.md — 160–200 lines, no JSON ABI, Solidity-style only.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/REQUIREMENTS.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-02-shared-references/02-CONTEXT.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-02-shared-references/RESEARCH.md
@/Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/shared-abi-fragments.md
@/Users/jaybuidl/project/kleros/kleros-skills/kleros-ipfs-upload/SKILL.md

<interfaces>
<!-- Source files to extract ABI content from. Read the specific line ranges below. -->
<!-- LGTCR §0.5 JSON ABI: curate-v1/curate-light-skill.md lines 124–184 -->
<!-- PGTCR §Minimal ABI: curate-v1/pgtcr-stake-curate-skill.md lines 562–641 -->
<!-- Scout §3 LightGeneralizedTCRView plain-text list: curate-v1-scout/scout-skills.md lines 128–176 -->

Key conversion rule (D-04, D-06):
  JSON: {"type":"function","name":"addItem","stateMutability":"payable","inputs":[{"name":"_item","type":"string"}],"outputs":[]}
  Solidity: function addItem(string _item) external payable

Key conversion rule (D-05):
  JSON: {"type":"event","name":"MetaEvidence","inputs":[{"indexed":true,"name":"_metaEvidenceID","type":"uint256"},{"indexed":false,"name":"_evidence","type":"string"}]}
  Solidity: event MetaEvidence(uint256 indexed _metaEvidenceID, string _evidence)
            // topic0 = keccak256("MetaEvidence(uint256,string)")
            //        = 0x61606860eb6c87306811e2695215385101daab53bd6ab4e9f9049aead9363c7d

Key PGTCR signature differences from LGTCR (do not confuse them):
  PGTCR: function addItem(string _item, uint256 _deposit) external payable   -- has _deposit param
  LGTCR: function addItem(string _item) external payable                      -- no _deposit param
  PGTCR uses challengeItem, not challengeRequest
  PGTCR has startWithdrawItem + withdrawItem (two-step); LGTCR has executeRequest
  PGTCR withdrawFeesAndRewards takes _challengeID uint120, not _requestID uint256

Required heading structure (predictable for Phase 3 cross-references):
  ## LightGeneralizedTCR
    ### Read functions
    ### Write functions
    ### Events
  ## PermanentGTCR
    ### Read functions
    ### Write functions
  ## LightGeneralizedTCRView
    ### Read functions
  ## IArbitrator
  ## Factory contracts
    ### LightGTCRFactory (LGTCR only)
    ### PermanentGTCRFactory (PGTCR only)

D-02 Scout pointer under ## LightGeneralizedTCRView:
  "(Scout registries use the LightGeneralizedTCRView helper at a fixed Gnosis address — see scout-registries.md for the hardcoded contract address.)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write shared-abi-fragments.md</name>
  <files>kleros-curate/references/shared-abi-fragments.md</files>
  <action>
Replace the stub content in shared-abi-fragments.md with the full ABI reference file.

Read the source sections first:
- curate-v1/curate-light-skill.md lines 124–184 (LGTCR §0.5 JSON ABI)
- curate-v1/pgtcr-stake-curate-skill.md lines 562–641 (PGTCR §Minimal ABI)
- curate-v1-scout/scout-skills.md lines 128–176 (Scout §3 LightGeneralizedTCRView)

Then write the file. Rules:

1. HEADING STRUCTURE — use exactly the canonical headings listed in the context interfaces block above. Phase 3 files will reference these headings by name. Do not invent alternate names.

2. FORMAT — All signatures in Solidity-style only (D-04, D-06). No JSON arrays anywhere. Each function on its own line. Visibility modifier: `external` for all public functions. State mutability: `view` for readers, `payable` for deposit-taking writers, `nonpayable` for state-mutating writers without ETH intake.

3. TOPIC0 CANONICAL LOCATION — The MetaEvidence topic0 hash 0x61606860eb6c87306811e2695215385101daab53bd6ab4e9f9049aead9363c7d belongs ONLY in this file, as a comment on the MetaEvidence event. No other shared file should contain the raw hash — they point here instead.

4. FLAVOR SUBSECTIONS (D-01) — Group by contract type, not by operation type. LightGeneralizedTCR (read + write + events), then PermanentGTCR (read + write), then LightGeneralizedTCRView, then IArbitrator, then Factory contracts.

5. FACTORY LABELING — Both factory ABIs go in ## Factory contracts. Label each subsection explicitly: "### LightGTCRFactory (LGTCR only)" and "### PermanentGTCRFactory (PGTCR only)". This answers the open question from RESEARCH.md: single ABI source wins over flavor-specificity.

6. SCOUT POINTER (D-02) — Under ## LightGeneralizedTCRView, open with the exact pointer text: "(Scout registries use the LightGeneralizedTCRView helper at a fixed Gnosis address — see scout-registries.md for the hardcoded contract address.)"

7. WRIT-01 (imperative form) — Opening lines and any prose instructions use imperative: "Use these signatures with ethers.js or viem — both parse Solidity-style ABI strings natively." Not: "These signatures can be used..."

8. WRIT-02 (explain WHY) — Add a brief rationale at the top of the file: why Solidity-style over JSON (smaller token footprint, native ethers/viem support, human-readable grep). One or two sentences is enough. Do not pad.

9. WRIT-04 (lean content) — Do not add prose commentary on every function. Signatures are self-documenting. Add inline comments only where a subtle gotcha exists (e.g., the _deposit param in PGTCR addItem, the two-step PGTCR withdrawal).

10. Do NOT extract into this file:
    - LightGTCRFactory deploy parameter documentation (Phase 3)
    - Sanity check guidance (Phase 3)
    - Any Scout registry addresses (Phase 3)
  </action>
  <verify>
    <automated>grep -c "function\|event" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/shared-abi-fragments.md</automated>
    <automated>grep -c "0x61606860" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/shared-abi-fragments.md</automated>
    <automated>grep -c "\[" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/shared-abi-fragments.md</automated>
  </verify>
  <done>
- File exists and is 160–200 lines
- grep for "function|event" returns at least 30 matches (covers all major signatures)
- grep for "0x61606860" returns exactly 1 match (topic0 in one place only)
- grep for "[" returns 0 matches (no JSON ABI arrays — square brackets only in array type params like bytes[] if any, which is acceptable; verify context)
- No "Phase 2 content here" placeholder text remains
- Headings match the canonical structure (## LightGeneralizedTCR, ## PermanentGTCR, ## LightGeneralizedTCRView, ## IArbitrator, ## Factory contracts)
- D-02 Scout pointer present under LightGeneralizedTCRView
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only phase; no runtime code, no user data, no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-KS02-SC | Tampering | No package installs in this plan | accept | Pure file editing; no npm/pip/cargo installs |
</threat_model>

<verification>
After the task completes:

1. File exists: `ls -la kleros-curate/references/shared-abi-fragments.md`
2. Line count in 160–200 range: `wc -l kleros-curate/references/shared-abi-fragments.md`
3. No JSON arrays: `grep -n '^\[' kleros-curate/references/shared-abi-fragments.md` — expect 0 results
4. Topic0 canonical: `grep -n "0x61606860" kleros-curate/references/shared-abi-fragments.md` — expect exactly 1 line
5. Heading structure: `grep -n "^##" kleros-curate/references/shared-abi-fragments.md`
6. No placeholder text: `grep -n "Phase 2 content here" kleros-curate/references/shared-abi-fragments.md` — expect 0 results
7. PGTCR addItem has _deposit param: `grep "addItem.*_deposit" kleros-curate/references/shared-abi-fragments.md`
8. Scout pointer present: `grep -n "Scout registries use" kleros-curate/references/shared-abi-fragments.md`
</verification>

<success_criteria>
- shared-abi-fragments.md is the single source of truth for all Curate ABI signatures
- All signatures are in Solidity-style; no JSON ABI arrays present
- MetaEvidence topic0 hash appears exactly once (in this file, as a comment on the event)
- Predictable heading names established for Phase 3 cross-references
- D-01/D-02/D-04/D-05/D-06 decisions implemented
- WRIT-01/WRIT-02/WRIT-04 writing standards applied
</success_criteria>

<output>
Create `.planning/phases/KS-02-shared-references/02-01-SUMMARY.md` when done
</output>
