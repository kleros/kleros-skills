---
phase: "03"
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - kleros-curate/references/scout-registries.md
autonomous: true
requirements:
  - FLAV-03

must_haves:
  truths:
    - "An agent loading scout-registries.md finds all 4 registry addresses (ATQ, Address Tags, Kleros Tokens, CDN) on Gnosis chainId 100"
    - "All 4 seed templates (JSON) are inline (~170 lines) — agents can seed item.json directly without guessing field structure (D-06)"
    - "LightGeneralizedTCRView helper address 0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53 is present with its function list"
    - "Image section is ~15 lines of Scout-specific requirements only — upload mechanics pointer to shared-ipfs-upload.md (D-07)"
    - "Incentives section contains no specific amounts — evergreen framing + blog.kleros.io redirect only (D-10)"
    - "Seed-first submission pattern explained with WHY (MetaEvidence as cross-check, not primary source)"
    - "Scout → Light Curate dependency banner present (also load references/light-curate.md)"
    - "No content from shared-*.md files duplicated (deposit formulas, eth_getLogs, item.json rules, IPFS mechanics)"
    - "All Phase 1 HTML source markers stripped"
    - "All '[Phase 3 content here]' placeholders replaced"
    - "File stays under 300 lines (D-05) — no TOC required"
  artifacts:
    - path: "kleros-curate/references/scout-registries.md"
      provides: "Scout context overlay for 4 Gnosis registries — addresses, seed templates, view helper, scout-api, image guidance, incentives"
      min_lines: 180
      max_lines: 299
  key_links:
    - from: "kleros-curate/references/scout-registries.md § The 4 registry addresses"
      to: "four Gnosis addresses"
      via: "hardcoded address strings"
      pattern: "0xAe6aaed\|0x66260C69\|0xeE1502e2\|0x957A53A9"
    - from: "kleros-curate/references/scout-registries.md § LightGeneralizedTCRView helper"
      to: "view helper address"
      via: "hardcoded address"
      pattern: "0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53"
    - from: "kleros-curate/references/scout-registries.md"
      to: "kleros-curate/references/light-curate.md"
      via: "top-of-file banner (also load light-curate.md)"
      pattern: "also load.*light-curate.md\|load.*light-curate"
---

<objective>
Fill kleros-curate/references/scout-registries.md with Scout-specific context for the 4 known Gnosis registries.

Purpose: An agent working with Scout registries loads this file for Scout-specific context (addresses, seed templates, view helper, API, image guidance, incentives) and loads light-curate.md separately for LGTCR contract operations. This is an overlay document, not a standalone operations manual — it adds Scout-specific knowledge on top of LGTCR operations.

Output: A filled scout-registries.md (~250-280 lines) using the organic overlay structure (D-04: addresses → seed templates → view helper → API → images → incentives), all 4 seed templates inline (D-06), image section ~15 lines (D-07), Scout submission checklist as standalone section (per Claude's Discretion recommendation in RESEARCH.md), incentive amounts replaced with blog link (D-10).
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-03-flavor-references/03-CONTEXT.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-03-flavor-references/03-RESEARCH.md

<interfaces>
<!-- Key section headings from Phase 1 stub — preserve exactly -->

From kleros-curate/references/scout-registries.md (current stub, 41 lines):
```
## Contents       ← TOC placeholder — remove (file will be under 300 lines)
## Overview
## The 4 registry addresses
## Seed-first submission pattern
## item.json templates per registry
## LightGeneralizedTCRView helper
## scout-api integration
## Image guidance
## Incentives (current campaign)
```

Additional section to add (Claude's Discretion — RESEARCH.md recommendation):
```
## Submission checklist   ← 14-item pre-flight checklist from draft §14
```

Scout 4 registry addresses (hardcoded — immutable deployed contracts per D-10):
```
Chain: Gnosis (chainId: 100)
ATQ (Address Tags Query) Registry: 0xAe6aaed5434244be3699c56E7Ebc828194F26dc3
Address Tags: 0x66260C69d03837016d88c9877e61e08Ef74C59F2
Kleros Tokens: 0xeE1502e29795Ef6C2D60F8D7120596abE3baD990
CDN (Ledger Contract Domain Name v2): 0x957A53A994860BE4750810131d9c876b2f52d6E1
```

LightGeneralizedTCRView helper (hardcoded — immutable deployment per D-10):
```
Address: 0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53
Functions:
  fetchArbitrable(address _address) → registry params batch
  getItem(address _address, bytes32 _itemID) → single item with request data
  getLatestRequestData(address _address, bytes32 _itemID) → request info
  getLatestRoundRequestData(address _address, bytes32 _itemID, uint256 _requestID) → round info
  availableRewards(address _address, address _contributor, bytes32 _itemID, uint256 _requestID, uint256 _roundID) → pending withdrawal
Usage rule: prefer view helper for read-heavy operations (batch fewer RPC calls).
Fallback: if helper returns stale data or reverts, fall back to direct registry reads.
```

scout-api endpoint (hardcoded per D-10 — endpoints):
```
POST https://scout-api.kleros.link/api/address-tags
Body: { address: "0x...", chains: [1, 100, ...] }
Limitation: scout-api is not canonical source — only use for pre-submission research.
Not a hard gate — submission proceeds even if scout-api returns no results.
```

The banner at top of scout-registries.md stub (preserve and refine):
```
> Scout IS LightGeneralizedTCR on Gnosis. This file covers Scout-specific context only.
> For all contract operations (submit item, challenge, evidence, appeal, factory deploy),
> also load `references/light-curate.md`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill scout-registries.md with Scout overlay context</name>
  <files>kleros-curate/references/scout-registries.md</files>
  <read_first>
    - kleros-curate/references/scout-registries.md (the stub — current state, 41 lines, all placeholders)
    - curate-v1-scout/scout-skills.md lines 1-67 (§intro + §Why this matters — overview + ecosystem context)
    - curate-v1-scout/scout-skills.md lines 27-55 (§Why people care — incentives framing)
    - curate-v1-scout/scout-skills.md lines 69-78 (§1 fixed scope — addresses)
    - curate-v1-scout/scout-skills.md lines 59-67 (seed-first summary)
    - curate-v1-scout/scout-skills.md lines 385-552 (§7 seed templates — all 4 JSON templates)
    - curate-v1-scout/scout-skills.md lines 120-181 (§3 contract model — view helper)
    - curate-v1-scout/scout-skills.md lines 329-343 (§6 Method 2 — view helper usage)
    - curate-v1-scout/scout-skills.md lines 604-627 (§8 optional research — scout-api)
    - curate-v1-scout/scout-skills.md lines 638-699 (§9 image guidance)
    - curate-v1-scout/scout-skills.md lines 969-985 (§14 submission checklist — 14 items)
    - curate-v1-scout/scout-skills.md lines 930-936 (§12 view helper — availableRewards)
    - kleros-curate/references/shared-ipfs-upload.md (understand what upload mechanics NOT to duplicate)
    - kleros-curate/references/shared-item-json.md (understand what JSON rules NOT to duplicate)
    - kleros-ipfs-upload/SKILL.md lines 36-48 (writing style reference)
  </read_first>
  <action>
Rewrite kleros-curate/references/scout-registries.md from scratch. Strip the Phase 1 stub entirely (remove all HTML source markers, TOC placeholder, "[Phase 3 content here]" placeholders). This is an overlay document — it adds Scout-specific knowledge on top of LGTCR operations that live in light-curate.md.

Keep the top-of-file dependency banner (refined):
```
> Scout IS LightGeneralizedTCR on Gnosis. This file covers Scout-specific context only.
> For all contract operations (submit item, challenge, evidence, appeal, factory deploy),
> also load `references/light-curate.md`.
```

Structure to produce (organic overlay order per D-04):

**# Scout Registries (Gnosis)**

Dependency banner immediately below title.

**## Overview**
Source: draft §intro lines 1-67 + blunt summary from §15. 2-3 paragraphs max:
- What Scout is: 4 curated registries on Gnosis Chain for contract and token tagging. ~700k+ entries. Used by Etherscan, Ledger/Blockscout for address labeling and token display.
- Scout IS LightGeneralizedTCR. Not a separate contract — the same LGTCR contracts under a specific overlay. Contract operations (submit, challenge, evidence, appeal, factory) are in light-curate.md.
- The seed-first inversion: when submitting to Scout registries, use the embedded seed template as the primary JSON source and MetaEvidence as cross-check — not the other way around. WHY: seed templates include the full columns+values shape and are more reliable than inferring structure from MetaEvidence alone. If seed and MetaEvidence disagree on labels/order/required fields: stop and escalate.

**## The 4 registry addresses**
Source: draft §1 lines 69-78. Table format:

| Registry | Address | Purpose |
|----------|---------|---------|
| ATQ (Address Tags Query) | 0xAe6aaed5434244be3699c56E7Ebc828194F26dc3 | Lookup existing address tags |
| Address Tags | 0x66260C69d03837016d88c9877e61e08Ef74C59F2 | Submit address → label mappings |
| Kleros Tokens | 0xeE1502e29795Ef6C2D60F8D7120596abE3baD990 | Submit token metadata |
| CDN (Contract Domain Names) | 0x957A53A994860BE4750810131d9c876b2f52d6E1 | Submit domain → contract address mappings |

All on Gnosis (chainId: 100). Native token for LGTCR deposits: xDAI.

**## Seed-first submission pattern**
Source: draft §15 blunt summary + §4 lines 59-67, 382-378. Explain the inversion clearly:

Traditional flow (do NOT use for Scout): fetch MetaEvidence → infer schema → build item.json.
Seed-first flow (correct for Scout): load seed template from this file → prefill values → fetch MetaEvidence as cross-check → if they agree: proceed. If they disagree: stop and ask.

WHY seed-first: "The embedded seed templates include the full {columns, values} JSON shape including field ordering, types, and required vs optional fields. MetaEvidence is the authoritative schema, but the seed is a pre-validated snapshot — using it first reduces the risk of field order mistakes or missing required fields." If MetaEvidence clearly disagrees (different column labels, added/removed required fields): stop and ask rather than guessing.

**## item.json templates per registry**
Source: draft §7 lines 385-552. ALL 4 SEED TEMPLATES INLINE (~170 lines per D-06).

Intro: "These are the default item.json shapes for each Scout registry. Use as starting point — replace PLACE_VALUE_HERE placeholders with actual values. Do not change column names or structure unless MetaEvidence cross-check reveals a discrepancy."

Include each template with a heading per registry:
- `### ATQ (Address Tags Query)` — seed JSON
- `### Address Tags` — seed JSON
- `### Kleros Tokens` — seed JSON
- `### CDN (Contract Domain Names)` — seed JSON

For item.json construction rules (deep-copy rule, values population, schema confirmation): `shared-item-json.md`.

**## LightGeneralizedTCRView helper**
Source: draft §3 lines 120-181 + §6 Method 2 lines 329-343 + §12 lines 930-936.

Address: `0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53` (immutable deployment — hardcode, no discovery needed).

Available functions (list with one-liner per function):
- `fetchArbitrable(address _address)` — batch-reads registry params (submissionBaseDeposit, arbitrator, etc.) in a single call; prefer over individual reads for efficiency
- `getItem(address _address, bytes32 _itemID)` — single item with full request data
- `getLatestRequestData(address _address, bytes32 _itemID)` — current request info
- `getLatestRoundRequestData(address _address, bytes32 _itemID, uint256 _requestID)` — round info for appeal state
- `availableRewards(address _address, address _contributor, bytes32 _itemID, uint256 _requestID, uint256 _roundID)` — pending withdrawal amount

Usage rule: prefer view helper for read-heavy operations — reduces RPC call count.
Fallback: if helper returns stale data or reverts, use direct `LightGeneralizedTCR` reads (see light-curate.md).

For the full view helper ABI: `shared-abi-fragments.md`.

**## scout-api integration**
Source: draft §8 lines 604-627.

Optional pre-submission research endpoint. POST to `https://scout-api.kleros.link/api/address-tags` with body `{ "address": "0x...", "chains": [1, 100, ...] }`.

Limitation: scout-api aggregates existing registry data but is not the canonical source. Use for pre-submission research to avoid duplicate submissions. NOT a hard gate — if scout-api is unavailable or returns no results, submission can proceed.

**## Image guidance**
Source: draft §9 lines 638-699 — Scout-specific requirements ONLY (~15 lines per D-07). Upload mechanics → shared-ipfs-upload.md.

Scout-specific requirements:
- Token logos (for Kleros Tokens registry): PNG format, minimum 128×128px, square aspect ratio. Transparent background preferred. File size: under 200KB.
- CDN visual proof (for Contract Domain Names registry): screenshot of the exact domain or subdomain page with the contract address visible in context (URL bar or page content). Must demonstrate the domain/subdomain authoritatively points to the contract address.
- ATQ / Address Tags: no image required.

For upload mechanics (gateway selection, /ipfs/ path format, durability): `shared-ipfs-upload.md`.

**## Incentives (current campaign)**
Source: draft §Why people care lines 27-55 — evergreen framing ONLY, strip specific figures (D-10).

Three profit paths (structural/evergreen):
1. Project visibility — verified entries surface in Etherscan labels, Ledger Live, and Blockscout address tagging. Increases ecosystem discoverability for projects.
2. Submitter upside — approved submissions earn a fraction of challenge deposits when challengers fail, over time.
3. Challenger upside — successful challenges on non-compliant submissions earn the submitter's deposit.

No specific amounts (D-10): "For current campaign terms, reward amounts, and active incentive programs: check blog.kleros.io."

WHY no amounts here: "Incentive figures change with governance votes. A specific number here becomes misleading the day the campaign changes — the blog post is always current."

**## Submission checklist**
Source: draft §14 lines 969-985 (14 items). Include as standalone section — high value density, pre-flight sanity check.

Intro: "Run through this before submitting to any Scout registry."

Include the 14-item checklist from the draft. Condensed format: one line per check. Items expected to cover: correct registry address confirmed, chainId is Gnosis (100), seed template loaded for target registry, field values filled (no PLACE_VALUE_HERE), JSON is valid, policy document read, image uploaded if required, deposit computed live (not cached), simulation passed, MetaEvidence cross-check performed, no duplicate entry in registry.

Writing rules: imperative/infinitive throughout (WRIT-01), explain WHY behind seed-first and image requirements (WRIT-02), no shared-file content duplicated, no HTML source markers (D-12), no TOC (under 300 lines per D-05), no specific incentive amounts (D-10).
  </action>
  <verify>
    <automated>wc -l /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md</automated>
  </verify>
  <acceptance_criteria>
    - File line count: between 180 and 299 (`wc -l` output in range)
    - All 4 registry addresses present: `grep -c "0xAe6aaed\|0x66260C69\|0xeE1502e2\|0x957A53A9" kleros-curate/references/scout-registries.md` returns 4
    - View helper address present: `grep -c "0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53" kleros-curate/references/scout-registries.md` returns >= 1
    - All 4 seed templates present: `grep -c "^### ATQ\|^### Address Tags\|^### Kleros Tokens\|^### CDN" kleros-curate/references/scout-registries.md` returns 4
    - Dependency banner present: `grep -c "also load.*light-curate.md\|light-curate.md.*also load" kleros-curate/references/scout-registries.md` returns >= 1
    - No specific incentive amounts: `grep -c "PNK/month\|300k\|[0-9].*PNK" kleros-curate/references/scout-registries.md` returns 0
    - blog.kleros.io redirect present: `grep -c "blog.kleros.io" kleros-curate/references/scout-registries.md` returns >= 1
    - scout-api endpoint hardcoded: `grep -c "scout-api.kleros.link" kleros-curate/references/scout-registries.md` returns >= 1
    - Image section concise: section between "## Image guidance" and next "##" heading is <= 20 lines (D-07)
    - No HTML source markers: `grep -c "<!-- Source:" kleros-curate/references/scout-registries.md` returns 0
    - No placeholders: `grep -c "Phase 3 content here\|TOC placeholder\|PLACE_VALUE_HERE" kleros-curate/references/scout-registries.md` returns 0 (PLACE_VALUE_HERE should only appear inside seed template JSON blocks — if checked, grep inside JSON templates is acceptable)
    - Seed-first pattern present: `grep -c "seed.*first\|seed-first\|seed template.*primary\|primary.*seed" kleros-curate/references/scout-registries.md` returns >= 1
    - Upload mechanics not duplicated: `grep -c "eth_getLogs\|fromBlock.*earliest" kleros-curate/references/scout-registries.md` returns 0
  </acceptance_criteria>
  <done>scout-registries.md contains the complete Scout overlay: all 4 addresses, all 4 seed templates inline, view helper with address and function list, scout-api endpoint, image guidance ~15 lines (Scout-specific only), incentives as evergreen framing with blog link, submission checklist, no HTML markers, no placeholders, under 299 lines</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed template → submitted item.json | Seed templates define the item shape; stale templates cause field-order mismatch with current MetaEvidence |
| incentive framing → agent behavior | Specific incentive amounts encourage agents to report stale figures to users; evergreen framing prevents this |
| scout-api → submission gating | scout-api labeled non-canonical so agents don't block submissions on its availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | Scout incentive amounts — stale figures mislead agents and users | mitigate | D-10: all specific amounts removed; replaced with blog.kleros.io redirect; acceptance criteria verify no "PNK/month" or "300k" patterns |
| T-03-11 | Tampering | Seed template field order diverges from MetaEvidence schema | accept | Seed-first pattern includes explicit cross-check step: if seed disagrees with MetaEvidence, stop and ask. Accepted risk: templates may become stale if governance updates schema |
| T-03-12 | Denial of Service | scout-api unavailability blocks submissions | mitigate | scout-api explicitly labeled optional and "not a hard gate" — submission proceeds even without API results |
| T-03-13 | Information Disclosure | Wrong PLACE_VALUE_HERE values in final JSON — mock data included in real submission | mitigate | Seed-first section instructs: replace all PLACE_VALUE_HERE before submitting; submission checklist item confirms no placeholders remain |
| T-03-14 | Repudiation | CDN image guidance — agent accepts screenshot without visible address context | mitigate | CDN image requirement explicitly states "address context visible in URL bar or page content" |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this phase — pure markdown authoring |
</threat_model>

<verification>
Run after plan execution:

```bash
# Line count in acceptable range
wc -l /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# All 4 registry addresses
grep -n "0xAe6aaed\|0x66260C69\|0xeE1502e2\|0x957A53A9" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# View helper address
grep -n "0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# All 4 seed template headings
grep -n "^### ATQ\|^### Address Tags\|^### Kleros Tokens\|^### CDN" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# Dependency banner
grep -n "also load.*light-curate\|light-curate.*also load" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# No incentive amounts
grep -n "PNK/month\|300k\|[0-9].*PNK" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# Blog redirect
grep -n "blog.kleros.io" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# Scout-api endpoint
grep -n "scout-api.kleros.link" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md

# No source markers
grep -c "<!-- Source:" /Users/jaybuidl/project/kleros/kleros-skills/kleros-curate/references/scout-registries.md
```
</verification>

<success_criteria>
FLAV-03 satisfied when:
1. scout-registries.md is 180-299 lines (no TOC needed)
2. All 4 Gnosis registry addresses present in a table
3. All 4 seed templates inline as JSON blocks under per-registry headings
4. LightGeneralizedTCRView address 0xB32e38B08FcC7b7610490f764b0F9bFd754dCE53 hardcoded with function list
5. scout-api endpoint hardcoded with limitation note (not a hard gate)
6. Image section <= 20 lines of Scout-specific requirements only; upload mechanics delegated to shared-ipfs-upload.md
7. Incentives section: zero specific figures; three evergreen profit paths + blog.kleros.io
8. Dependency banner: "also load references/light-curate.md"
9. Seed-first pattern explains WHY (MetaEvidence as cross-check, not primary source)
10. Zero HTML source markers, zero "[Phase 3 content here]" placeholders
</success_criteria>

<output>
Create `.planning/phases/KS-03-flavor-references/03-03-SUMMARY.md` when done
</output>
