---
phase: KS-04-triggering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kleros-curate/SKILL.md
autonomous: true
requirements:
  - TRIG-01
  - TRIG-02
  - TRIG-03

must_haves:
  truths:
    - "YAML description field is ≥1,300 and ≤1,536 characters (measured by wc -c on the description string)"
    - "Description contains all required positive trigger terms: Curate, Light Curate, LGTCR, PGTCR, Stake Curate, PermanentGTCR, Scout, registry, token list, address tags, CDN, addItem, removeItem, challengeItem, challengeRequest"
    - "Description explicitly states generic IPFS uploads without Curate context belong to kleros-ipfs-upload"
    - "Description uses three-pass structure: Use when / Also trigger when / Even if"
    - "Description excludes arbitrator-layer terms: MetaEvidence, arbitrationCost, evidence submission, submission deposit, challenge deposit"
    - "Description includes implicit trigger clause covering registry operations combined with Kleros context signals"
    - "Description includes override exception clause for explicit skill invocation or testing"
    - "SKILL.md body (line 5 onward) is unchanged"

  artifacts:
    - path: "kleros-curate/SKILL.md"
      provides: "Updated YAML frontmatter with rewritten description field"
      contains: "description:"
      min_lines: 4

  key_links:
    - from: "kleros-curate/SKILL.md description field"
      to: "all three Curate flavors"
      via: "trigger keyword coverage (LGTCR, PGTCR, Scout terms)"
      pattern: "LGTCR.*PGTCR.*Scout|Scout.*LGTCR.*PGTCR"
---

<objective>
Rewrite the `description:` value in `kleros-curate/SKILL.md` YAML frontmatter using a three-pass aggressive trigger style that covers all three Curate flavors within the 1,536-character cap.

Purpose: The description field is the sole triggering mechanism for Claude Code. Current description (726 chars) under-triggers on workflow-verb and situational-phrase patterns because it lists only noun keywords. The three-pass expansion saturates the trigger surface for developers (contract function names), non-developers (natural language verbs), and implicit contexts (registry operations + Kleros signals).

Output: `kleros-curate/SKILL.md` with rewritten `description:` field. Body unchanged.
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/ROADMAP.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/phases/KS-04-triggering/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite YAML description field with three-pass aggressive trigger style</name>
  <files>kleros-curate/SKILL.md</files>

  <read_first>
    - kleros-curate/SKILL.md — lines 1-4 (current description, 726 chars; the ONLY field modified)
    - kleros-ipfs-upload/SKILL.md — lines 1-4 (1,147-char published reference; models three-pass structure, boundary language, override exception clause)
    - .planning/phases/KS-04-triggering/04-CONTEXT.md — decisions D-01 through D-08 (authoritative trigger architecture)
  </read_first>

  <action>
    Replace only the `description:` value on line 3 of kleros-curate/SKILL.md. Do not touch lines 1-2, 4, or any line past the closing `---` on line 4.

    The new description must implement D-01 through D-08 from 04-CONTEXT.md exactly:

    Pass 1 — "Use this skill when..." — list protocol names and contract-level identifiers: Curate, Light Curate, LGTCR, LightGeneralizedTCR, LightCurate, PGTCR, PermanentGTCR, Stake Curate, Scout, token list, address tags, CDN tags, Goldsky, addItem, removeItem, challengeItem, challengeRequest (per D-03). Include all four Curate flavors explicitly. (~400 chars)

    Pass 2 — "Also trigger when the user wants to..." — workflow verbs and situational phrases (per D-01): submit an item to a registry, challenge a submission, remove an item, appeal a dispute, deploy a new Curate registry, curate a list, browse registry entries, check whether an address is tagged, add a token to a token list, query a curated list, fund an appeal round. (~350 chars)

    Pass 3 — "Even if Curate is not mentioned, trigger when..." — implicit trigger clause (per D-06): the user describes registry operations (adding entries to a list, checking item status, browsing items, querying a decentralized list) combined with Kleros context signals (Kleros, arbitrator, dispute, juror, PNK). Then the negative trigger (per D-07): do NOT trigger for generic IPFS uploads with no Curate context — those belong to kleros-ipfs-upload. Then the override exception (per D-08): if the user explicitly names the kleros-curate skill, or asks to test/validate this skill, trigger regardless of topical context. (~350 chars)

    Exclusions (per D-02): do NOT include MetaEvidence, arbitrationCost, evidence submission, submission deposit, challenge deposit anywhere in the description — these are arbitration-layer terms shared across all Kleros products.

    Character target: 1,300–1,500 chars for the description string value (per D-05). Count only the description text, not the YAML key.

    YAML validity: the description value must be a valid YAML block scalar. If the text contains colons followed by spaces, wrap the entire value in a YAML literal block scalar (`description: |`) or a double-quoted string. Prefer a literal block scalar for readability if line breaks help readability; otherwise a plain scalar is fine as long as no YAML special characters appear unescaped.
  </action>

  <verify>
    <automated>
      # 1. Description field exists and YAML frontmatter is intact
      grep -c "^description:" kleros-curate/SKILL.md

      # 2. All required positive trigger terms present (TRIG-02)
      python3 -c "
import re, sys
text = open('kleros-curate/SKILL.md').read()
# Extract description block (between first --- and second ---)
fm = re.search(r'^---\n(.*?)\n---', text, re.DOTALL)
desc = fm.group(1) if fm else ''
required = ['Curate', 'Light Curate', 'LGTCR', 'PGTCR', 'Stake Curate', 'PermanentGTCR', 'Scout', 'registry', 'token list', 'address tags', 'CDN', 'addItem', 'removeItem', 'challengeItem', 'challengeRequest']
missing = [t for t in required if t not in desc]
print('MISSING:', missing) if missing else print('ALL TERMS PRESENT')
"

      # 3. Description length within 1,300-1,536 chars
      python3 -c "
import re
text = open('kleros-curate/SKILL.md').read()
fm = re.search(r'^---\n(.*?)\n---', text, re.DOTALL)
desc_block = fm.group(1) if fm else ''
# Extract description value (handles plain scalar, quoted, or literal block)
m = re.search(r'description:\s*[|>]?\s*\n?(.*)', desc_block, re.DOTALL)
desc_val = m.group(1).strip() if m else ''
# For multiline literal block, join lines
desc_val = re.sub(r'\n\s+', ' ', desc_val).strip()
n = len(desc_val)
print(f'Description length: {n} chars')
assert 1300 <= n <= 1536, f'FAIL: {n} chars not in [1300, 1536]'
print('PASS')
"

      # 4. Excluded terms absent (D-02)
      python3 -c "
text = open('kleros-curate/SKILL.md').read()
import re
fm = re.search(r'^---\n(.*?)\n---', text, re.DOTALL)
desc = fm.group(1) if fm else ''
forbidden = ['MetaEvidence', 'arbitrationCost', 'submission deposit', 'challenge deposit']
found = [t for t in forbidden if t in desc]
print('FORBIDDEN TERMS FOUND:', found) if found else print('NO FORBIDDEN TERMS — PASS')
"

      # 5. Body unchanged — line count of body (line 5+) must be same as before this edit
      # Current body starts at line 6 (after closing ---)
      # Verify body line 1 still reads: "# Kleros Curate"
      awk 'NR==6{print}' kleros-curate/SKILL.md | grep -q "# Kleros Curate" && echo "BODY INTACT" || echo "BODY CHANGED — FAIL"
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "^description:" kleros-curate/SKILL.md returns count 1
    - All 15 required terms from TRIG-02 (Curate, Light Curate, LGTCR, PGTCR, Stake Curate, PermanentGTCR, Scout, registry, token list, address tags, CDN, addItem, removeItem, challengeItem, challengeRequest) appear in the description
    - Description string length is ≥1,300 and ≤1,536 characters
    - None of MetaEvidence, arbitrationCost, submission deposit, challenge deposit appear in the description block
    - Line 6 of kleros-curate/SKILL.md still reads "# Kleros Curate" (body untouched)
    - Three-pass markers appear: text contains "Use this skill when" or equivalent Pass 1 opener, a Pass 2 section covering workflow verbs, and a Pass 3 implicit trigger clause
    - Negative trigger for kleros-ipfs-upload present: description contains "kleros-ipfs-upload"
    - Override exception present: description contains language about explicit skill naming or testing triggering regardless of context
  </acceptance_criteria>

  <done>kleros-curate/SKILL.md description field is 1,300-1,536 characters, covers all TRIG-02 terms, excludes D-02 forbidden terms, follows three-pass structure, and the SKILL.md body is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → SKILL.md | Plain text edit; no execution, no secrets, no external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | kleros-curate/SKILL.md frontmatter | accept | Single-file plain-text edit; no secrets, no deps, no network; content reviewed by human on /gsd:verify |
| T-04-02 | Information Disclosure | YAML description field | accept | Description is intentionally public-facing — no PII, no credentials |
</threat_model>

<verification>
After execution:

1. Run all automated verify commands in Task 1 — all must pass.
2. Character count: 1,300 ≤ len(description) ≤ 1,536.
3. Required terms check: all 15 TRIG-02 terms present.
4. Forbidden terms check: none of the D-02 arbitration-layer terms appear.
5. Body integrity: line 6 is "# Kleros Curate".
6. YAML validity: `python3 -c "import yaml; yaml.safe_load(open('kleros-curate/SKILL.md').read().split('---\n')[1])"` exits 0.
</verification>

<success_criteria>
- YAML description length: 1,300–1,536 chars
- All TRIG-02 positive triggers present: Curate, Light Curate, LGTCR, PGTCR, Stake Curate, PermanentGTCR, Scout, registry, token list, address tags, CDN, addItem, removeItem, challengeItem, challengeRequest
- TRIG-03 negative trigger present: explicit exclusion of generic IPFS uploads pointing to kleros-ipfs-upload
- D-02 terms absent: MetaEvidence, arbitrationCost, submission deposit, challenge deposit not in description
- Three-pass structure present: Use when / Also trigger when / Even if implicit trigger
- D-08 override exception clause present
- kleros-curate/SKILL.md body (line 6+) byte-identical to pre-edit state
</success_criteria>

<output>
Create `.planning/phases/KS-04-triggering/04-01-SUMMARY.md` when done.
</output>
