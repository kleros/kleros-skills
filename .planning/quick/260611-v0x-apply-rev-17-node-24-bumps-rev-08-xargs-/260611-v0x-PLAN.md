---
phase: quick-260611-v0x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/sync-master.yml
  - CLAUDE.md
autonomous: true
requirements:
  - REV-17
  - REV-08
  - REV-10
  - REV-13

must_haves:
  truths:
    - "sync-master.yml uses SHA-pinned Node-24-compatible actions for all three action pins"
    - "FEEDBACK/HANDOVER strip uses null-delimited pipeline (xargs -0, grep -z, git ls-files -z)"
    - "Dead idempotency block ('No changes after strip') is absent from the workflow"
    - "CLAUDE.md branch model section documents that release tags reference dev commits, not master"
    - "npm test passes after all edits (no YAML parse errors introduced)"
  artifacts:
    - path: ".github/workflows/sync-master.yml"
      provides: "Hardened sync workflow"
      contains: "actions/create-github-app-token@bcd2ba49"
    - path: "CLAUDE.md"
      provides: "Branch model doc with REV-13 bullet"
      contains: "Release tags reference dev commits"
  key_links:
    - from: ".github/workflows/sync-master.yml"
      to: "actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1"
      via: "uses: field on line 46"
      pattern: "create-github-app-token@bcd2ba49"
    - from: ".github/workflows/sync-master.yml"
      to: "git ls-files -z"
      via: "strip step null-delimited pipeline"
      pattern: "git ls-files -z"
---

<objective>
Apply four hardening items from 260607-w84 REVIEW.md: bump three GitHub Actions to Node-24-compatible
SHA-pinned versions (REV-17), harden the FEEDBACK/HANDOVER strip with null-delimited xargs (REV-08),
remove the unreachable idempotency check (REV-10), and document the dev-commit tag architecture in
CLAUDE.md (REV-13).

Purpose: REV-17 has a hard deadline of 2026-06-16 — the runner forces Node 24 that day, potentially
breaking the sync workflow. REV-08, REV-10, and REV-13 are bundled as low-cost improvements in the
same pass.

Output: Updated .github/workflows/sync-master.yml and CLAUDE.md, committed (no push — orchestrator
handles that).
</objective>

<execution_context>
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaybuidl/project/kleros/kleros-skills/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/STATE.md
@/Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
@/Users/jaybuidl/project/kleros/kleros-skills/CLAUDE.md
@/Users/jaybuidl/project/kleros/kleros-skills/.planning/quick/260607-w84-execute-seed-002-branch-based-minimal-st/260607-w84-REVIEW.md

<interfaces>
<!-- REV-17 target SHA pins (resolved live 2026-06-11 per REVIEW.md) -->
<!-- actions/create-github-app-token: fee1f7d... (v2.2.2) → bcd2ba49... (v3.2.0) -->
<!-- actions/checkout:               @v4 → 93cb6efe18208431cddfb8368fd83d5badbf9bfd (v6.0.3) -->
<!-- actions/setup-node:             @v4 → a0853c24544627f65ddf259abe73b1d18a591444 (v6.4.0) -->
<!-- Pre-bump verification COMPLETE (orchestrator confirmed): -->
<!--   - app-slug output still present at v3.2.0 ✓ -->
<!--   - No breaking input changes that affect app-id / private-key ✓ -->
<!--   - v3.1.0 deprecates app-id in favour of client-id, but app-id still works — do NOT rename -->

<!-- REV-08 null-delimited strip pipeline (current lines 155-156 in sync-master.yml): -->
<!-- BEFORE: -->
<!--   git ls-files | grep -E '^[^/]*FEEDBACK[^/]*\.md$|^HANDOVER[^/]*\.md$' | \ -->
<!--     xargs --no-run-if-empty git rm --quiet -->
<!-- AFTER: -->
<!--   git ls-files -z | \ -->
<!--     grep -zE '^[^/]*FEEDBACK[^/]*\.md$|^HANDOVER[^/]*\.md$' | \ -->
<!--     xargs -0 --no-run-if-empty git rm --quiet -->

<!-- REV-10 dead block to remove (current lines 214-220 in sync-master.yml): -->
<!--   # If nothing changed after strip, master is already in sync — skip. -->
<!--   STAGED=$(git diff --cached --name-only | wc -l) -->
<!--   UNSTAGED=$(git diff --name-only | wc -l) -->
<!--   if [ "$STAGED" -eq 0 ] && [ "$UNSTAGED" -eq 0 ]; then -->
<!--     echo "No changes after strip — master is already up to date." -->
<!--     exit 0 -->
<!--   fi -->

<!-- REV-13 bullet to add in CLAUDE.md (after the "master is NOT a git ancestor" bullet, line 50): -->
<!--   - Release tags reference dev commits, not master — `git tag --contains HEAD` on a master checkout finds nothing. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply REV-17 + REV-08 + REV-10 to sync-master.yml</name>
  <files>.github/workflows/sync-master.yml</files>
  <action>
Read the current sync-master.yml. Apply three sets of edits in one pass:

REV-17 — replace three action pins (do NOT change any input names, only the `uses:` SHA):
  Line 46: change `actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349  # v2.2.2`
            to     `actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1  # v3.2.0`
  Line 55: change `actions/checkout@v4`
            to     `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v6.0.3`
  Line 101: change `actions/setup-node@v4`
            to     `actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444  # v6.4.0`
  Also update the step name on line 100 from "Set up Node 20" to "Set up Node 24" AND change
  `node-version: "20"` to `node-version: "24"` on line 103. Rationale: Node 20 entered EOL on
  2026-04-30 (current LTS is Node 24, since Oct 2025). Full Node 24 migration keeps the workflow
  consistent — step name, action runtime, and installed runtime all match.

REV-08 — replace the two-line strip pipeline (lines 155-156) with the three-line null-delimited form:
  BEFORE (2 lines):
    git ls-files | grep -E '^[^/]*FEEDBACK[^/]*\.md$|^HANDOVER[^/]*\.md$' | \
      xargs --no-run-if-empty git rm --quiet
  AFTER (3 lines):
    git ls-files -z | \
      grep -zE '^[^/]*FEEDBACK[^/]*\.md$|^HANDOVER[^/]*\.md$' | \
      xargs -0 --no-run-if-empty git rm --quiet
  The regex pattern itself is unchanged. GNU grep on ubuntu-latest supports -z.

REV-10 — remove the unreachable idempotency block from the "Commit stripped tree and push to master" step.
  The block starts with the comment "# If nothing changed after strip..." and ends after `exit 0` and `fi`.
  Remove all six lines (comment + STAGED= + UNSTAGED= + if condition + echo + exit + fi).
  The step's next line after the removal is `git commit -m "chore(master): sync from dev (strip dev-only files)"`.
  Do not touch any other lines in the step.

After editing, run: `npm test` — it must pass. This validates YAML syntax is intact (Node built-in test runner
would not run at all if the workflow file caused a parse error in the test suite; confirm it at least executes).

Commit with:
  git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
chore(sync): REV-17 bump actions to Node 24 + SHA-pin; REV-08 null xargs; REV-10 drop dead check

- actions/create-github-app-token: v2.2.2 → v3.2.0 (SHA-pinned)
- actions/checkout: @v4 → v6.0.3 (SHA-pinned)
- actions/setup-node: @v4 → v6.4.0 (SHA-pinned); rename step to "Set up Node 24"
- node-version: "20" → "24" (Node 20 past EOL 2026-04-30; Node 24 current LTS)
- REV-08: git ls-files -z | grep -z | xargs -0 in FEEDBACK/HANDOVER strip
- REV-10: removed unreachable "No changes after strip" early-exit block

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
  </action>
  <verify>
    <automated>
npm test
grep -n "actions/create-github-app-token@bcd2ba49" /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
grep -n "actions/checkout@93cb6efe" /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
grep -n "actions/setup-node@a0853c24" /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
grep -n 'node-version: "24"' /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
grep -n "git ls-files -z" /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml
grep -c "No changes after strip" /Users/jaybuidl/project/kleros/kleros-skills/.github/workflows/sync-master.yml || true
    </automated>
  </verify>
  <done>
All three grep checks return the new SHA pins. "git ls-files -z" appears once. "No changes after strip" does
not appear (grep -c returns 0). npm test passes (exit 0). Commit is in git log.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply REV-13 doc bullet to CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
Read CLAUDE.md. In the `## Branch model` section, find the **Rules:** bullet list. The current bullets are:
  - All PRs target `dev`.
  - Never commit or push directly to `master` — it is regenerated automatically.
  - `master` is NOT a git ancestor of `dev`; `git log master` will not match `dev` history.
  - Sync fires on tag push matching `*@v*.*.*` or `v[0-9]*`, and on `workflow_dispatch`.
  - Strip-list ...
  - Every workflow run ...

Insert a new bullet AFTER the "`master` is NOT a git ancestor" line and BEFORE the "Sync fires" line:
  - Release tags reference dev commits, not master — `git tag --contains HEAD` on a master checkout finds nothing.

Keep the telegraphic style matching existing bullets (no trailing period inconsistency — check what the
other bullets use and match it).

Commit with:
  git -c commit.gpgsign=false commit -m "$(cat <<'EOF'
docs(claude-md): REV-13 note that release tags reference dev commits, not master

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
  </action>
  <verify>
    <automated>
grep -A 3 "Release tags reference dev" /Users/jaybuidl/project/kleros/kleros-skills/CLAUDE.md
    </automated>
  </verify>
  <done>
grep returns the new bullet with the expected text about `git tag --contains HEAD`. Commit is in git log.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow YAML → GitHub Actions runner | Action version pins must reference exact SHAs; mutable tags are a supply-chain vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v0x-01 | Tampering | `uses:` SHA pins in sync-master.yml | mitigate | SHAs resolved live per REVIEW.md; pre-bump verification confirmed by orchestrator before this plan was written |
| T-v0x-02 | Tampering | `app-id` input on create-github-app-token | accept | v3.1.0 deprecated `app-id` but still accepts it; orchestrator confirmed app-slug output present; no credential logic change in this task |
| T-v0x-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this task; edits are file-only |
</threat_model>

<verification>
After both commits:
- `git log --oneline -3` shows two commits with the correct messages
- `grep -n "create-github-app-token@bcd2ba49" .github/workflows/sync-master.yml` → one match (line ~46)
- `grep -n "checkout@93cb6efe" .github/workflows/sync-master.yml` → one match (line ~55)
- `grep -n "setup-node@a0853c24" .github/workflows/sync-master.yml` → one match (line ~101)
- `grep -c "No changes after strip" .github/workflows/sync-master.yml` → 0
- `grep -c "git ls-files -z" .github/workflows/sync-master.yml` → 1
- `grep "Release tags reference dev" CLAUDE.md` → one match
- `npm test` passes
</verification>

<success_criteria>
- sync-master.yml: all three action pins updated to Node-24-compatible SHA-pinned versions
- sync-master.yml: FEEDBACK/HANDOVER strip uses null-delimited pipeline (ls-files -z | grep -z | xargs -0)
- sync-master.yml: "No changes after strip" dead-code block is gone
- CLAUDE.md: one new bullet in Branch model Rules list documenting release-tag / master-commit mismatch
- npm test passes
- Two clean commits in git log, GPG signing disabled, Co-Authored-By present
- No push to origin (orchestrator handles that)
</success_criteria>

<output>
Create `.planning/quick/260611-v0x-apply-rev-17-node-24-bumps-rev-08-xargs-/260611-v0x-SUMMARY.md` when done.
</output>
